# Force Windows line endings (CRLF) for all text files
* text=auto eol=crlf encoding=UTF-8
*.lib binary

# Preserve exact upstream and patch bytes for the LuaCOM import.
/luacom/** -text
/third_party/luacom/** -text
